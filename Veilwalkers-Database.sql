create database veilwalkers;

use veilwalkers;

create table email(
id int not null auto_increment,
nombre varchar(64),
email varchar(64),
primary key (id)
);

create table beta(
id int not null auto_increment,
nombre varchar(64),
email varchar(64),
primary key (id)
);